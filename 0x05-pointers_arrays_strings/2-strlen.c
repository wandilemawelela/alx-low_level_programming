#include "main.h"
#include <string.h>
/**
  * _strlen - returns the length of a string
  * @s: String
  * Return: Returns the length of the string
  *
  */

int _strlen(char *s)
{
	return (strlen(s));
}
